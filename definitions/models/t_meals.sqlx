config {
  type: "incremental",
  schema:"dataform",
  uniqueKey: ["meal_id"]
}

SELECT meal_id, meal_name, calories,
CAST('${dataform.projectConfig.vars.execution_date}' AS DATE) as execution_date
FROM 
${ref("meals")}
where date= '${dataform.projectConfig.vars.execution_date}'